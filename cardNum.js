import cardNum from './numToStar'

function cardNum(y) {
    if(y.length === 16) {
        return cardNum(y)
    }else {
        return "잘못 입력하셨습니다."
    }
}

import cardNum from './numToStar'

function cardNum(y) {
    if(y.length === 16) {
        return cardNum(y)
    }else {
        return "잘못 입력하셨습니다."
    }
}